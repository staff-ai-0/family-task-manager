# INERT — the backend image now builds from the REPO ROOT context (compose
# sets `context: .`, `dockerfile: backend/Dockerfile`; see that file's header
# comment), and Podman/Docker only honor a .dockerignore next to the build
# context root. This per-directory file is never read. Add excludes to the
# root .dockerignore instead.
