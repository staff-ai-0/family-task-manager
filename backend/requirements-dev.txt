# Development and test dependencies — NOT installed in the production image.
# Install with: pip install -r requirements-dev.txt
# (which installs requirements.txt first via the -r directive)
-r requirements.txt

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Code Quality — ruff is the single linter (config in backend/ruff.toml).
# Replaced black/flake8/mypy/isort 2026-07-16: none had config files and none
# ran in CI; ruff check now gates CI instead.
ruff==0.15.12
