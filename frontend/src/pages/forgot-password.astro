---
import Layout from "../layouts/Layout.astro";

const lang = Astro.cookies.get("lang")?.value ?? "en";

const copy = {
  es: {
    title: "Olvidé mi contraseña",
    heading: "Recuperar contraseña",
    subtitle: "Te enviaremos un enlace de restablecimiento a tu correo.",
    label: "Correo electrónico",
    btn: "Enviar enlace",
    sending: "Enviando...",
    success: "Si existe una cuenta con ese correo, recibirás el enlace en unos minutos. Revisa también tu carpeta de spam.",
    back: "Volver al inicio de sesión",
  },
  en: {
    title: "Forgot password",
    heading: "Recover your password",
    subtitle: "We'll send a reset link to your email address.",
    label: "Email address",
    btn: "Send reset link",
    sending: "Sending...",
    success: "If an account with that email exists, you'll receive the link shortly. Check your spam folder too.",
    back: "Back to login",
  },
}[lang] ?? {};
---

<Layout title={`${copy.title} — Family Task Manager`}>
  <main class="min-h-screen flex items-center justify-center p-4 bg-brand-cream-deep">
    <div class="w-full max-w-md bg-brand-cream rounded-[var(--radius-card)] border-2 border-brand-ink shadow-[var(--shadow-pop)] p-8">
      <div class="mb-8 text-center">
        <div class="inline-flex items-center justify-center w-14 h-14 rounded-full bg-indigo-50 mb-4">
          <svg class="w-7 h-7 text-brand-coral-deep" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z" />
          </svg>
        </div>
        <h1 class="text-2xl font-bold text-brand-ink">{copy.heading}</h1>
        <p class="text-brand-ink-soft mt-1 text-sm">{copy.subtitle}</p>
      </div>

      <div id="success-msg" class="hidden mb-6 p-4 rounded-lg bg-brand-mint/20 border border-brand-mint text-brand-mint-deep text-sm text-center">
        {copy.success}
      </div>

      <form id="forgot-form" class="space-y-5">
        <div id="error-msg" class="hidden p-4 rounded-lg bg-red-100 border border-red-200 text-red-700 text-sm"></div>
        <div>
          <label for="email" class="block text-sm font-medium text-brand-ink mb-1">{copy.label}</label>
          <input
            type="email" id="email" name="email" required autocomplete="email"
            class="w-full px-4 py-3 rounded-lg border border-brand-ink/20 focus:ring-2 focus:ring-brand-coral focus:border-brand-coral outline-none transition-shadow-[var(--shadow-card)]"
          />
        </div>
        <button
          type="submit" id="submit-btn"
          class="w-full py-3 px-4 rounded-lg text-white font-medium bg-brand-coral-deep hover:bg-brand-ink transition-colors shadow-[var(--shadow-card)]"
        >
          {copy.btn}
        </button>
      </form>

      <div class="mt-6 text-center">
        <a href="/login" class="text-sm text-brand-coral-deep hover:text-brand-coral-deep font-medium transition-colors">
          &larr; {copy.back}
        </a>
      </div>
    </div>
  </main>

  <script define:vars={{ sending: copy.sending, btnLabel: copy.btn }}>
    document.addEventListener('astro:page-load', () => {
    const form = document.getElementById("forgot-form");
    const btn = document.getElementById("submit-btn");
    const successMsg = document.getElementById("success-msg");
    const errorMsg = document.getElementById("error-msg");

    form?.addEventListener("submit", async (e) => {
      e.preventDefault();
      const email = document.getElementById("email").value;

      btn.disabled = true;
      btn.textContent = sending;
      errorMsg.classList.add("hidden");

      try {
        const res = await fetch("/api/auth/forgot-password", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ email }),
        });

        // Always show success regardless of whether email exists (anti-enumeration)
        form.classList.add("hidden");
        successMsg.classList.remove("hidden");
      } catch {
        errorMsg.textContent = "An error occurred. Please try again.";
        errorMsg.classList.remove("hidden");
        btn.disabled = false;
        btn.textContent = btnLabel;
      }
    });
    });
  </script>
</Layout>
