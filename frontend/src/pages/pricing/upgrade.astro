---
// Redundant with /parent/settings/subscription — that page is the canonical
// upgrade surface (plans + current + usage + cancel; linked from settings and
// the UpgradePrompt), a superset of this leaner plan/checkout page which had no
// inbound link anywhere. Redirect old bookmarks there instead of maintaining
// two overlapping billing surfaces.
return Astro.redirect("/parent/settings/subscription", 301);
---
