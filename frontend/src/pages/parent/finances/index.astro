---
// The standalone "Family Finances Hub" was superseded by /budget (Phase 10,
// native budget). It had no inbound link anywhere; redirect any old bookmarks
// to the canonical budget home instead of maintaining a duplicate surface.
return Astro.redirect("/budget/", 301);
---
