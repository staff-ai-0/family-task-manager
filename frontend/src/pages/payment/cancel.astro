---
import Layout from "../../layouts/Layout.astro";

const lang = Astro.cookies.get("lang")?.value ?? "en";
---

<Layout title="Payment Cancelled - Family Task Manager">
    <main class=" bg-gradient-to-br from-slate-50 to-slate-100 flex items-center justify-center px-4">
        <div class="max-w-2xl w-full bg-brand-cream rounded-[var(--radius-card)] border-2 border-brand-ink shadow-[var(--shadow-pop)] p-8">
            <div class="text-center">
                <div class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-brand-sun/20 mb-6">
                    <svg
                        class="h-10 w-10 text-yellow-600"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"
                        ></path>
                    </svg>
                </div>
                
                <h1 class="text-3xl font-bold text-brand-ink mb-4">
                    Payment Cancelled
                </h1>
                
                <p class="text-lg text-brand-ink-soft mb-8">
                    You cancelled the payment process. No charges were made to your account.
                </p>

                <div class="bg-brand-cream-deep rounded-lg p-6 mb-8">
                    <p class="text-brand-ink">
                        If you encountered any issues or have questions about our pricing,
                        please don't hesitate to <a href="/contact" class="text-brand-sky-deep hover:text-brand-sky-deep font-medium">contact us</a>.
                    </p>
                </div>

                <div class="flex flex-col sm:flex-row gap-4 justify-center">
                    <a
                        href="/payment"
                        class="inline-flex items-center justify-center px-6 py-3 rounded-lg bg-brand-sky-deep hover:bg-brand-ink text-white font-medium transition-colors shadow-[var(--shadow-card)]"
                    >
                        Try Again
                    </a>
                    <a
                        href="/dashboard"
                        class="inline-flex items-center justify-center px-6 py-3 rounded-lg bg-brand-cream-deep hover:bg-brand-cream-deep text-brand-ink font-medium transition-colors"
                    >
                        Back to Dashboard
                    </a>
                </div>
            </div>
        </div>
    </main>
</Layout>
