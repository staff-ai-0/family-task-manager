# API base URL for the backend
# Used by server-side Astro API routes to call the backend
# In Docker: http://backend:8000 (internal network)
# In development: http://localhost:8002 (for local testing)
# In production: Set via docker-compose environment variable
API_BASE_URL=http://localhost:8002

# Port for Astro dev server
PORT=3000
