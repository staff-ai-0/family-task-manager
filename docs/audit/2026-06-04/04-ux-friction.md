# UX Friction — Budget / Task / Gigs (Phase 2+3)

Goal: simplest possible workflows.

Total: 21 findings. Confirmed-real: 7, refuted: 0, unverified: 14.


## [ux-budget]

### [HIGH] "Categories" nav links are dead — point to a #categories accordion that does not exist in settings  — ✅REAL
- Domain: budget · Category: ux · Effort: M
- Evidence: DrawerMenu.astro:27 → `{ emoji: "🏷️", label: "Categories", href: "/budget/settings#categories" }` and budget/index.astro:154 empty-state → `<a href="/budget/settings#categories">Create categories</a>`. But settings.astro only defines accordions id=accounts/payees/rules/recurring/goals/tags/ai-scanner/backups (settings.astro:97-417) — there is NO `id="categories"` accordion. SettingsAccordion.astro:76-93 auto-opens by hash, so `#categories` resolves to nothing: the user lands on Settings with every section collapsed and no category UI. The ONLY place to add a category is a tiny unlabeled `+` icon on expense group headers in the month view (BudgetCategoryGroup.astro:75-83), and there is no UI anywhere to create a category GROUP.
- Impact: Two of the most prominent category entry points (hamburger menu + the dashboard's own empty-state CTA) are broken. A new parent who clicks "Create categories" from the empty month view hits a dead settings page and is stuck. Category management is effectively undiscoverable.
- Fix: Add a `Categories` accordion to settings.astro (id="categories") with full group+category CRUD (the data is already fetched at settings.astro:39 via /api/budget/categories/groups), mirroring the accounts/payees pattern. Point both the drawer link and the index empty-state at it. Surface a labeled "+ New category group" / "+ Category" button instead of the bare `+` icon.
- Verify: CONFIRMED and stronger than claimed. DrawerMenu.astro:27 links Categories→/budget/settings#categories; budget/index.astro:154 empty-state links the same. settings.astro renders 8 SettingsAccordion ids (accounts/payees/rules/recurring/goals/tags/ai-scanner/backups, lines 97-417) with NO id="categories". SettingsAccordion.astro:76-93 auto-opens by hash; a non-matching #categories opens nothing, leaving every section collapsed. The standalone page budget/categories/index.astro is itself just `Astro.redirect("/budget/settings#categories", 301)` — so it dead-ends too. Additionally the BudgetCategoryGroup.astro:75-84 "+" icon (quick-add-btn) the finding cites as the only category-add path has NO click handler anywhere in the repo (grep: quick-add-btn appears only in that markup), so it is a non-functional button. There is no UI to create a category GROUP. Net: no working path to create a category or group.

### [HIGH] No way to create the first Account from the add-transaction or empty flows — it is buried in Settings  — ·unverified
- Domain: budget · Category: ux · Effort: M
- Evidence: Account create UI exists ONLY at settings.astro:97-135 (`openAccountModal()` behind the collapsed Accounts accordion at /budget/settings#accounts). The FAB transaction form requires an account (FABModal.astro:301-306 throws errAccount if blank) and renders a bare `<option value="">--</option>` with no accounts (FABModal.astro:131-136). The transactions empty-state (transactions.astro:210-219) and the dashboard empty-state (budget/index.astro:149-158) only mention categories, never accounts. There is no first-run onboarding (grep for onboard/setup-wizard/getting-started across budget pages = 0 hits).
- Impact: A brand-new parent opening Budget sees an empty month, taps the big + FAB to add an expense, and cannot save because there are no accounts and no inline way to make one — they must independently discover hamburger → Settings → expand Accounts accordion → New account. High drop-off at the very first action.
- Fix: Add an empty-state onboarding card on the dashboard when accounts.length===0 (e.g. "Add your first account" → opens the account modal inline). Let the FAB/transaction account dropdown offer an inline "+ New account" option. A short 2-step first-run wizard (account → done; categories already auto-seed at category_service.py:111-116) would remove the entire cold-start cliff.


## [ux-gig]

### [HIGH] Two parallel "gig" systems are both surfaced to users with the SAME vocabulary — kid has two unrelated "gigs" UIs  — ·unverified
- Domain: gig · Category: ux · Effort: L
- Evidence: Kid dashboard renders is_bonus task-assignments as gigs: dashboard.astro:253-367 ('Bonus Tasks' section, gig-proof modal at :428-466, trust-streak text 'Your gigs auto-approve instantly!' :102-103, gig_mode badges 'competition/rotation/collaboration' :292-308). SEPARATELY, BottomNav.astro:46-53 has a 'Gigs' tab → /gigs/index.astro which is the NEW GigOffering system (apiFetch('/api/gigs/offerings') :19, claim form :110, /gigs/my-gigs.astro proof form :110-140). Same word 'gigs', two different claim flows, two different proof-submission UIs, two different 'awaiting approval' lists. GigsIntroBanner i18n (i18n.ts:439) even calls is_bonus tasks 'Gigs (bonus tasks)' while the nav 'Gigs' tab is a totally different feature.
- Impact: A kid finishing mandatory tasks sees a 'Bonus Tasks' card unlock on the dashboard AND a 'Gigs' tab in the nav — both promise extra points, both need proof, both await approval, but they're backed by different tables/endpoints with different rules. Maximal confusion about 'where do I earn points'. The intro banner tries to paper over it but conflates the two.
- Fix: Pick ONE gig surface. Recommended: retire is_bonus templates from the kid dashboard (the CLAUDE.md commit log shows GigOffering is the intended successor 'separate gig_offerings/gig_claims from mandatory tasks'). Move all bonus-earning into the /gigs board, leave the dashboard for mandatory tasks only, and drop the 'Bonus Tasks' dashboard section + gig-proof modal. Reword the intro banner to point at the single /gigs board.

### [HIGH] Parent has TWO separate approval screens for what they call "gigs" — and the dashboard badge misses half of them  — ·unverified
- Domain: gig · Category: ux · Effort: M
- Evidence: parent/approvals.astro is titled 'Gig approvals' (:40) but loads '/api/task-assignments/pending-approvals' (:18-21) — i.e. the LEGACY is_bonus gigs. parent/gigs.astro 'Pending' tab (:136-192) loads '/api/gigs/claims/pending-approvals' (:20) — the NEW gig claims. Parent dashboard (parent/index.astro:172-194) links an 'Approvals'/'Review pending gigs' tile whose badge count `pendingApprovals` comes ONLY from '/api/task-assignments/pending-approvals' (:24), so it does NOT count new gig-board claims. The new gig approvals are only reachable via the bottom-nav 'Gigs' tab → Pending sub-tab.
- Impact: A parent who approved everything on the 'Approvals' screen still has un-reviewed kid work sitting in the Gigs→Pending tab with no badge on the dashboard. Kids' submitted gig-board proofs can silently rot unapproved, so points are never credited and the kid thinks the parent is ignoring them. Two screens for one mental model ('approve my kids' work').
- Fix: Merge into a single 'Approvals' inbox that unions both '/api/task-assignments/pending-approvals' and '/api/gigs/claims/pending-approvals' (tag each row with its source), and make the parent dashboard badge sum both counts. As the underlying systems consolidate (finding 1), this collapses naturally to one list.


## [ux-task]

### [HIGH] Two parallel 'extra work' systems with duplicate create/claim/approve UIs confuse parents and kids  — ✅REAL
- Domain: task · Category: ux · Effort: XL
- Evidence: A parent can create extra-earning work via TWO unrelated flows: (a) TaskCreateModal.astro:209-221 + 263-277 — toggle 'Bonus task' + pick a 'Gig mode' on a TaskTemplate (POST /api/task-templates/), which surfaces to kids in dashboard.astro:253-335 (the 'Bonus' section); (b) parent/gigs.astro:199-256 — a separate 'New Gig' modal (POST /api/gigs/offerings) surfacing to kids in gigs/index.astro. There are TWO kid surfaces (dashboard 'Bonus' tasks vs the /gigs board), TWO approval queues (parent/approvals.astro reading /api/task-assignments/pending-approvals vs parent/gigs.astro tab=pending reading /api/gigs/claims/pending-approvals), and TWO point models. Both label points '1 pt = $1 MXN' but bonus-task points and gig points are set in completely different ways (see separate finding).
- Impact: A parent who wants to 'pay my kid to wash the car' has no obvious single path — they may use the bonus-task toggle in /parent/tasks or the New Gig button in /parent/gigs, landing the work on different kid screens with different approval inboxes. Kids must check both the dashboard Bonus section AND the /gigs tab. Approvals split across /parent/approvals and /parent/gigs?tab=pending means a parent can miss pending work. This is the single biggest source of task-UX complexity.
- Fix: Pick ONE system for extra/paid work. Given the gig board is the newer, purpose-built surface (separate model, cash framing, its own nav tab), deprecate 'bonus' TaskTemplates: remove the Bonus toggle + Gig mode from TaskCreateModal/edit, hide bonus assignments from the dashboard, and route all extra work through /gigs. Consolidate the two approval inboxes into one /parent/approvals fed by both sources during migration, then collapse to gig claims only.
- Verify: CONFIRMED. Two fully parallel systems exist. (a) Bonus TaskTemplate path: TaskCreateModal.astro:212-220 (Bonus toggle) + 263-277 (Gig mode select) POSTs to /api/task-templates/ (line 525), surfaces to kids in the dashboard Bonus section (dashboard.astro:252-253, filtered by template_is_bonus at line 31), with approvals fed by /api/task-assignments/pending-approvals (parent/index.astro:24, approvals.astro:19). (b) Gig board: parent/gigs.astro:43-48 'New Gig' button + modal POSTing to /api/gigs/offerings (line 311), surfacing to kids at /gigs/index.astro:19 (/api/gigs/offerings), with its own pending tab reading /api/gigs/claims/pending-approvals (gigs.astro:20). Two kid surfaces, two approval queues, two point models, plus a 4th parent nav destination (BottomNav.astro:110 -> /parent/gigs). One overstatement: the claim that BOTH label points '1 pt = $1 MXN' is only half true — only the gig board carries MXN/peso framing (gigs.astro:40 'cash rewards', gigs/index.astro:53 'worth real pesos', :99 '${o.points} MXN'); the bonus-task path has no MXN framing, it just sets points:10 (TaskCreateModal.astro:506). Core finding (duplicate parallel systems) is solidly real; severity high is appropriate given the genuine user-facing confusion across 2 surfaces + 2 inboxes.


## [ux-budget]

### [MEDIUM] FAB quick-add mode buttons are mislabeled: "Photo" opens receipt scan, "Scan" opens CSV import  — ✅REAL
- Domain: budget · Category: ux · Effort: S
- Evidence: FABModal.astro:74-93 renders three mode buttons: `⌨️ Manual` (no-op stays on form), `📸 {fab_photo}` → href=`/budget/scan-receipt`, and `📄 {fab_scan}` → href=`/budget/import`. i18n.ts:422-423 / 856-857: `fab_photo: "Photo"/"Foto"`, `fab_scan: "Scan"/"Scan"`. So the button literally labeled "Scan" navigates to CSV Import, while the camera/scan feature is labeled "Photo". The Spanish "Scan" is also left untranslated.
- Impact: Users tap "Scan" expecting the AI receipt scanner and land on a CSV file-import form; the actual scanner is mislabeled "Photo". Directly undermines the headline receipt-scan feature and confuses the primary add-transaction surface.
- Fix: Relabel: `📸 Scan receipt` (→/budget/scan-receipt) and `📄 Import file` (→/budget/import). Fix the Spanish `fab_scan` string. Consider dropping the redundant `Manual` toggle since the form is already manual by default.
- Verify: CONFIRMED. FABModal.astro:81-92 renders `📸 {fab_photo}` linking href=/budget/scan-receipt and `📄 {fab_scan}` linking href=/budget/import. i18n.ts:422-423 fab_photo="Photo", fab_scan="Scan"; ES at 856-857 fab_photo="Foto", fab_scan="Scan" (left untranslated). So the button literally labeled "Scan" navigates to CSV Import while the receipt camera/scan feature is labeled "Photo". The redundant no-op `⌨️ Manual` toggle (FABModal.astro:75-80) is also present as claimed.

### [MEDIUM] Navigation is triple-fragmented: 3 top tabs + 13-item drawer + receipt icon + FAB + bottom nav, with duplicated destinations  — ·unverified
- Domain: budget · Category: ux · Effort: M
- Evidence: Every budget page renders BudgetNavNew (3 tabs Month/Transactions/Reports + a separate receipt-drafts icon: BudgetNavNew.astro:17-94), DrawerMenu (13 links across 4 sections incl. Month/Transactions/Reports AGAIN: DrawerMenu.astro:14-47), FABButton+FABModal, AND the app-wide BottomNav (active="budget"). Month/Transactions/Reports appear in BOTH the top tabs and the drawer. Tools (scan/import/recurring/rules) live only in the drawer behind a hamburger.
- Impact: Same destinations reachable from 2-3 places while core tools (scan receipt, import, recurring) are hidden one extra tap deep behind a hamburger. Cognitive load is high and the redundancy makes the IA feel inconsistent (e.g., Scan Receipt is in the drawer AND the FAB AND a header camera icon on transactions.astro:130).
- Fix: Drop the Month/Transactions/Reports duplication from the drawer (keep them only as top tabs). Reserve the drawer strictly for management/tools (Accounts, Categories, Payees, Scan, Import, Recurring, Rules, Settings). Or fold the whole drawer into the Settings page and replace the hamburger with a single Settings gear.

### [MEDIUM] Allocate-budget flow is hidden behind an unlabeled "Ready to Assign" number  — ·unverified
- Domain: budget · Category: ux · Effort: S
- Evidence: The AssignFundsModal (the envelope-budgeting core action) is triggered from the BudgetMonthNav ready-to-assign control (budget/index.astro:98-107, 169-175) and from per-category `.assign-pill-btn` clicks (AssignFundsModal.astro:255-260). There is no explicitly labeled "Assign"/"Budget" button — the user must intuit that tapping the dollar figure in the blue header opens the allocation modal. The modal also derives the month from the URL path `/budget/month/YYYY/MM` (AssignFundsModal.astro:190-195), but that route 301-redirects to `/budget/?year=&month=` (month/[year]/[month].astro), so on the canonical dashboard URL the path-parse fails and it silently falls back to the current month — assigning to the wrong month when the user is viewing a past/future month.
- Impact: The single most important budgeting action (moving Ready-to-Assign into category envelopes) is non-obvious, and when used on a non-current month it can write the allocation to today's month instead of the viewed one — a correctness-adjacent UX trap.
- Fix: Add an explicit labeled "Assign funds" button next to the Ready-to-Assign figure. Read the viewed month from the query string (`?year/?month`) rather than the path, since the path form is a redirect alias, so allocations always target the displayed month.


## [ux-gig]

### [MEDIUM] Kid claim→proof journey is split across two pages with a dead-end hop instead of inline submission  — ·unverified
- Domain: gig · Category: ux · Effort: M
- Evidence: On /gigs/index.astro, claiming a gig immediately redirects to /gigs/my-gigs (index.astro:154 `window.location.href = '/gigs/my-gigs'`). A claimed-but-not-submitted gig on the board shows only a link 'Claimed — Submit proof →' (:117-121) that bounces the kid to my-gigs.astro, where the proof form actually lives (:109-140). The board card can't submit proof itself; the kid must navigate away to a second page to finish the very gig they just claimed.
- Impact: Three navigations (browse → claim-redirect → find card on my-gigs → submit) for one logical action. The board and my-gigs duplicate the gig card UI with subtly different states, doubling the surface a kid must learn.
- Fix: Make claim open the proof form inline on the board card (the my-gigs form is ~30 lines of markup — inline it on /gigs into an expandable card), or at minimum auto-scroll/anchor to the just-claimed gig on my-gigs. Eliminate the 'go look on the other page' hop.

### [MEDIUM] Parent gig-create modal omits the gig MODE — modes only exist on legacy is_bonus templates, so the board can't express its own headline feature  — ·unverified
- Domain: gig · Category: ux · Effort: M
- Evidence: parent/gigs.astro create/edit modal (:206-256) only collects title, description, points, difficulty, category — no gig-mode selector. Yet the kid dashboard renders 'competition / rotation / collaboration' mode badges (dashboard.astro:292-308) that come from template_gig_mode on the legacy is_bonus path. So the four advertised gig modes (claim/rotation/competition/collaboration) are only creatable via the OLD task-template editor, not the new Gigs management screen.
- Impact: Parents using the dedicated 'Gigs' screen can only ever create plain claim-one gigs; to make a rotation/competition/collab gig they must go to a different editor (task templates with is_bonus). The 'New Gig' button under-delivers vs. what the product advertises, reinforcing the two-system split.
- Fix: Either (a) add a mode selector to the gig-create modal and back it in GigOffering, unifying modes under the new board, or (b) explicitly scope the gig board to single-claim gigs and remove mode badges from the kid dashboard. Don't advertise four modes the primary creation surface can't make.

### [MEDIUM] "1 pt = $1 MXN" cash framing is shown everywhere but no cash payout exists — sets up a trust/expectation failure  — ·unverified
- Domain: gig · Category: ux · Effort: M
- Evidence: Cash framing is pervasive: parent/gigs.astro header 'Post extra tasks with cash rewards' (:40), offering card '${o.points} MXN' (:107), pending '${claim.gig_points} MXN' (:159); kid /gigs/index.astro 'Earn extra money!' (:43), 'points worth real pesos' (:53), '${o.points} MXN' (:99); my-gigs.astro approved '${claim.points_awarded} MXN' (:97). But per the codebase map there is no cash-payout backend — MONEY rewards redeem through the normal rewards flow (rewards.astro redeems via /api/rewards/{id}/redeem only). The MXN number is pure UI fiction with no withdrawal path.
- Impact: Kids see explicit peso amounts on every gig and an approved '+$50 MXN' badge, but nothing in the app lets them actually receive pesos. When real money never materializes, the feature feels like a broken promise and erodes trust in the whole points economy — worse for a kid audience.
- Fix: Either (a) make the MXN→payout real (a parent 'mark as paid' action on approved gigs that logs the cash handoff), or (b) soften the copy to 'worth up to $X in rewards' and surface the actual redemption path. Don't print a peso figure next to 'Approved' unless a payout exists.


## [ux-task]

### [MEDIUM] Create modal silently sets non-bonus task points to 0; only fixable on a separate edit page  — ✅REAL
- Domain: task · Category: ux · Effort: S
- Evidence: TaskCreateModal.astro:506 — `points: isBonus ? 10 : 0, // non-bonus tasks always earn 0 pts; editable via edit page`. The modal has no points field for regular tasks at all (the 'Difficulty ×1/×1.5/×2' chips at 144-154 are a multiplier over a base of 0). So a parent creating a normal chore through the primary '+ New Task' flow produces a task worth 0 points unless they later navigate to /parent/tasks/[id]/edit (edit.astro:162-175) to set a points value.
- Impact: Parents reasonably expect chores to be worth points (the whole app is points-based). Creating a chore the fast way yields a silently worthless task; the difficulty multiplier the parent picked multiplies 0. There is no in-modal indication that points are 0, so the misconfiguration is invisible until a kid completes the chore and earns nothing.
- Fix: Add a points input (or quick presets like 5/10/20) directly in step 2 of TaskCreateModal for all tasks, defaulting to a sensible non-zero base. Show the effective points (base × difficulty multiplier) live so the multiplier chips have visible meaning.
- Verify: CONFIRMED exactly as described. TaskCreateModal.astro:506 — `points: isBonus ? 10 : 0, // non-bonus tasks always earn 0 pts; editable via edit page`. The modal's step 2 has NO points field anywhere (fields are name :130, difficulty chips :144-154, frequency :162-172, assign-to :180-190, bonus toggle :210-221, plus advanced desc/roles/gig-mode/blocks-rewards). The difficulty chips are a multiplier (×1/×1.5/×2) over the base, and renderTemplateCard at line 623 computes effectivePts = pts * multiplier — so with base 0, a regular task shows +0 pts. The only points input is on the edit page (edit.astro:167-174, min=1 max=1000). So a parent creating a normal chore via '+ New Task' produces a 0-point task until they navigate to the edit page. Real medium UX defect.

### [MEDIUM] Gig-only jargon (Gig mode, Competition/Rotation/Collaboration) leaks into mandatory-task creation/edit  — ✅REAL
- Domain: task · Category: ux · Effort: M
- Evidence: TaskCreateModal.astro:263-291 exposes a 'Gig mode' select (claim/competition/rotation/collaboration) and 'Blocks rewards' toggle inside the New Task modal, and edit.astro:297-318 repeats the same 'Gig mode' select on the task edit form — even though tasks.astro:72 explicitly filters bonus tasks OUT of this page ('gigs live in /parent/gigs'). So the task page hides gigs from the list but still shows gig-config fields in its create/edit forms. assignment_type (auto/fixed/rotate) at TaskCreateModal.astro:180-190 AND gig_mode (claim/rotation/competition/collaboration) at 268-276 are two overlapping 'who does this / how is it shared' concepts presented side by side.
- Impact: Parents configuring a simple daily chore are shown competition/collaboration/rotation options that only make sense for the gig board, plus a redundant second rotation concept (assignment_type=rotate vs gig_mode=rotation). The cognitive load of two distribution models on one form is high and the settings have no effect for non-bonus tasks, so it is dead UI that still demands attention.
- Fix: Remove 'Gig mode' and 'Blocks rewards' from the mandatory-task create modal and edit page; keep only assignment_type (auto/fixed/rotate). Move gig-distribution modes entirely into the gig-creation modal (parent/gigs.astro), which currently does NOT even expose them (it only has title/points/difficulty/category), so the modes are mis-located today.
- Verify: CONFIRMED. TaskCreateModal.astro:263-277 exposes a 'Gig mode' select (claim/competition/rotation/collaboration) and :279-291 a 'Blocks rewards' toggle inside the New Task modal (under Advanced options). edit.astro:297-318 repeats the same 'Gig mode' select on the task edit form, plus blocks_rewards at :320+. Meanwhile tasks.astro:72-73 explicitly filters is_bonus tasks OUT of the page ('// Only show mandatory tasks (is_bonus=False) — gigs live in /parent/gigs'). So the task page hides gigs from its list yet still shows gig-config fields in its create/edit forms. assignment_type (auto/fixed/rotate, TaskCreateModal.astro:180-190) and gig_mode (claim/rotation/competition/collaboration, :268-276) are two overlapping who-does-this/how-shared concepts side by side. And the dedicated gig-creation modal (parent/gigs.astro:206-256) does NOT expose these modes at all — only title/description/points/difficulty/category — confirming the modes are mis-located. Real medium.

### [MEDIUM] Parent hub fragments one mental model ('manage my kids' work') across 3-4 tiles with overlapping labels  — ·unverified
- Domain: task · Category: ux · Effort: M
- Evidence: parent/index.astro renders separate tiles for 'Tasks' (line 52, label 'Tasks' / 'Create & manage tasks', i18n.ts:82-83), 'Approvals' (line 172, 'Review pending gigs'), and 'Calendar' (line 195, parent_assignments_label='Calendar' / 'Weekly assignment view', i18n.ts:90-91) — and the BottomNav adds a 4th 'Gigs' destination for parents (BottomNav.astro:118-126 → /parent/gigs). 'Tasks' (templates) vs 'Calendar' (the concrete weekly assignments at parent/assignments.astro) are two views of the same data that a parent must learn are different things; 'Approvals' only covers task-assignment gigs, while gig-claim approvals live behind the separate Gigs tab.
- Impact: A parent must internalize that templates, the weekly calendar, gig offerings, task approvals, and gig approvals are five distinct screens. The label 'Calendar' for what is internally 'assignments' and 'Approvals: Review pending gigs' (which is really task-assignment proofs, not gig-board claims) actively mislead. New parents cannot predict where a given action lives.
- Fix: Group under one 'Chores & Gigs' hub: a single landing page with tabs (Templates | This Week | Approvals). Rename 'Calendar' to 'This Week' and fold both approval inboxes into one 'Approvals' tab. Reduce the parent hub grid from 3 task tiles to 1.

### [MEDIUM] Assignment creation is driven by a manual 'Weekly Shuffle' button — non-obvious, destructive, and hard to reason about  — ·unverified
- Domain: task · Category: ux · Effort: L
- Evidence: tasks.astro:131-173 — the only way concrete daily assignments come into existence is a 'Weekly Shuffle' section: a 'Preview' button (calls /api/assignments/preview) plus a 'Shuffle' submit guarded by onclick confirm() (line 158). The i18n copy (i18n.ts:139-141) is 'Weekly Shuffle' / 'Randomly assign templates to family members for this week'. Until a parent clicks Shuffle, the kid dashboard (dashboard.astro:21 /progress) and the parent Calendar (assignments.astro) show nothing. The shuffle also regenerates the whole week, and the assignment edit modal (assignments.astro:497-503) only offers reassign / reschedule / cancel — there is no 'add a single assignment' affordance.
- Impact: The core loop — 'I made a chore, now my kid sees it today' — does not happen automatically. A parent who creates templates but never finds/presses the Shuffle button gets an empty experience for the whole family. 'Shuffle' implies randomness/disruption (reinforced by the confirm dialog), discouraging parents from running the one action that makes the app work. Re-running mid-week to add one chore risks reshuffling everything.
- Fix: Auto-generate the upcoming week's assignments on a schedule (and immediately when a template is created/activated) so the kid dashboard is never empty. Rename 'Shuffle' to 'Generate this week' and reserve true reshuffle as a secondary action. Add a direct 'Assign a one-off task' button on the Calendar that creates a single TaskAssignment without touching the rest of the week.


## [ux-budget]

### [LOW] Settings is a 1117-line megapage with 8 accordions all collapsed by default and no search  — ·unverified
- Domain: budget · Category: ux · Effort: M
- Evidence: settings.astro is 1117 lines hosting Accounts, Payees, Rules, Recurring, Goals, Tags, AI/Scanner, Backups (settings.astro:97-437). SettingsAccordion defaults `defaultOpen=false` (SettingsAccordion.astro:14) and none are passed defaultOpen, so a user arriving at bare /budget/settings sees 8 closed rows with zero content and must guess which to expand. Deep-links rely on hash auto-open (SettingsAccordion.astro:76-93); arriving without a hash gives no default-open section.
- Impact: Everything management-related is funneled into one long collapsed list, so common tasks (add account, edit a rule) take an extra expand tap and there's no way to scan or search. Mobile users scroll a tall stack of identical-looking closed bars.
- Fix: Auto-open the first (Accounts) accordion when no hash is present. Group the 8 sections under 2-3 labeled headers (Money / Automation / Data). Consider splitting Accounts and Categories back into their own short pages reachable directly, instead of hash anchors into one giant file.

### [LOW] Edit/create flows hard-reload the whole page (location.reload) instead of updating in place  — ✅REAL
- Domain: budget · Category: ux · Effort: M
- Evidence: Transaction save/delete: transactions.astro:649 `window.location.reload()` and :681. Settings CRUD (accounts/payees/rules/recurring/goals/tags): every handler ends in `setTimeout(() => location.reload(), 800)` (settings.astro:637, 647, 680, 690, 776, 786, 888, 898...). AssignFundsModal.astro:212 `setTimeout(() => window.location.reload(), 800)`.
- Impact: Every add/edit/delete triggers a full SSR round-trip + scroll-position loss + visible flash, and the success toast is shown for only 800ms before the page reloads from under the user. On a slow mobile connection each tiny edit feels like a page navigation.
- Fix: Update the affected row/list item in the DOM on success (the API already returns the created/updated object) and skip the reload, or at minimum drop the artificial 800ms delay. Keeps the user's scroll position and makes rapid multi-edit sessions feel instant.
- Verify: CONFIRMED. transactions.astro:649 (save) and :681 (delete) call window.location.reload(). settings.astro CRUD handlers end in setTimeout(()=>location.reload(),800) at 637,647,680,690,776,786,888,898 (and more). AssignFundsModal.astro:212 setTimeout(()=>window.location.reload(),800). API responses already return the created/updated object, so in-place DOM update is feasible. Low severity (perf/UX polish).

### [LOW] Reports "Budget vs Actual" tab is a permanent "Coming soon" dead-end  — ✅REAL
- Domain: budget · Category: prod-gap · Effort: S
- Evidence: reports.astro:268-277: the `analysis` sub-tab renders only a static `Coming soon` / `Budget vs. actual analysis will be available soon.` placeholder. It is still presented as a first-class tab in ReportSubTabs alongside Spending/Cashflow/Net Worth (reports.astro:42-43, 87).
- Impact: A quarter of the Reports surface is an empty promise. Users tap a real-looking tab and get nothing, eroding trust in the reports area.
- Fix: Either hide the analysis tab until implemented, or wire it to the existing budget-vs-allocation data already available from the month endpoint (totals.budgeted vs activity per category are computed on the dashboard at budget/index.astro:66-67).
- Verify: CONFIRMED. reports.astro:267-278 renders the `analysis` sub-tab as a static "Coming soon" / "Budget vs. actual analysis will be available soon." placeholder. ReportSubTabs.astro:19 presents it as a first-class pill (label "vs Budget"/"vs Presupuesto") alongside Spending/Cashflow/Net Worth, and reports.astro:42-43,87 wire the tab. The budget-vs-allocation data is already computed elsewhere (index.astro:66-67). Minor label nuance: the tab reads "vs Budget", not literally "Budget vs Actual". Low severity (dead-end UX).


## [ux-gig]

### [LOW] Claim button loses its label on error in Spanish — JS reset hardcodes English "Claim"  — ·unverified
- Domain: gig · Category: ux · Effort: S
- Evidence: gigs/index.astro:158 on claim error does `btn.textContent = btn.getAttribute('data-label') ?? 'Claim'`, but the Claim button (:112-114) has NO data-label attribute — it renders 'Reclamar' in Spanish. So after a failed claim, the button text becomes literal English 'Claim' for a Spanish-locale kid, and the busy state is the ellipsis '...' set at :151.
- Impact: Minor but visible i18n regression on the unhappy path: Spanish users see an English button after any claim error (e.g. a gig already claimed by a sibling in competition mode — exactly when errors are most likely).
- Fix: Add `data-label={lang === 'es' ? 'Reclamar' : 'Claim'}` to the button at gigs/index.astro:112, or restore textContent from a captured original string instead of a hardcoded fallback.

### [LOW] Gig approve/reject and claim use raw window.confirm/prompt/alert and full-page reloads — jarring vs. the polished task-approval screen  — ·unverified
- Domain: gig · Category: ux · Effort: M
- Evidence: parent/gigs.astro reject uses `prompt(...)` for the reason (:347) and approve/archive use `alert('Error...')` + `window.location.reload()` (:319,328,341,355); archive uses `confirm(...)` (:325). By contrast parent/approvals.astro (the task path) removes the card in-place on success (li.remove() :171) with an inline notes textarea (:118-123) and only reloads when the list empties. The two approval UIs for 'the same job' feel built by different hands.
- Impact: Every gig approval reloads the whole page (loses scroll position, re-fetches everything), and rejection reasons go through a browser prompt() that can't be styled or validated. Inconsistent, lower-quality interaction than the sibling task-approval screen the parent also uses.
- Fix: Reuse the approvals.astro pattern on the gig Pending tab: inline notes textarea per card, optimistic card removal on success, toast instead of alert(), no full reload. Ideally the merged approval inbox (finding 2) makes this a single shared component.


## [ux-task]

### [LOW] Legacy Task model leaves stale terminology and a confusing template-vs-task mental split in the parent UI  — ·unverified
- Domain: task · Category: ux · Effort: M
- Evidence: The page titled 'Manage Tasks' (i18n pt_title, tasks.astro:107) actually manages TaskTemplates — the list items are templates (tasks.astro:182 templateList.map, subtitle 'N templates in family' i18n.ts:98), with Toggle/Edit/Delete acting on /api/task-templates/. Meanwhile the kid-facing word for the same concept is 'task' (dashboard 'Today'/'Required'). The repo also carries a third, obsolete legacy Task model (per the codebase map, routes unregistered) whose vocabulary ('task') collides with both templates and assignments. The 'ES missing' orange badge (tasks.astro:189-193) further clutters the parent's list with internal translation-state plumbing.
- Impact: Parents see 'template' in one place and 'task' in another for the same thing, and the developer-facing 'ES missing' badge surfaces i18n debt as user-visible noise. The conceptual stack (template → shuffle → assignment, plus a dead legacy Task) is more layers than a chore app needs, and the inconsistent naming makes support and onboarding harder.
- Fix: Standardize user-facing vocabulary: call templates 'Chores' (the reusable definition) and assignments 'Today's chores'/'This week'. Drop the word 'template' from parent-facing copy. Replace the 'ES missing' badge with a non-blocking, parent-invisible internal flag (or auto-translate on save without surfacing state). Remove the dead legacy Task model so it can't reintroduce 'task' ambiguity.

